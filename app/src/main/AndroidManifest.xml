<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.temproject.shipper">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.ResetPasswordActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.LoginActivity" />
        <activity
            android:name=".Activity.Shipper.SPHomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.Shipper.SPRegisterActivity" />
        <activity android:name=".Activity.Store.STRegisterActivity" />
        <!--<activity android:name=".Activity.Shipper.SPDetailRequestActivity"></activity>-->
        <!-- <activity android:name=".Activity.Store.STRegisterActivity" /> -->
         <!--<activity android:name=".Activity.Store.STRegisterActivity" />-->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- <activity -->
        <!-- android:name=".Activity.Shipper.MapsActivity" -->
        <!-- android:label="@string/title_activity_maps"></activity> -->
        <activity android:name=".Activity.Shipper.SPDetailRequestActivity" />
        <activity android:name=".Activity.ActiveAccountActivity" />
        <activity android:name=".Activity.ConfirmEmailActivity" />
        <!--<activity-->
            <!--android:name=".Activity.ResetPasswordActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->
        <activity
            android:name=".Activity.Store.STHomeActivity"
            android:label="@string/title_activity_sthome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.Store.STCreateRequestActivity" />
        <activity android:name=".Activity.Store.STDetailRequestActivity"></activity>
    </application>

</manifest>